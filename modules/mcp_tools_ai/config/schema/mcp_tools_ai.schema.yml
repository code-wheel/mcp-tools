mcp_tools_ai.settings:
  type: config_object
  label: 'MCP Tools — AI Function Calls settings'
  mapping:
    exposed_operations:
      type: sequence
      label: 'Exposed tool operations'
      sequence:
        type: string
        label: 'Operation'
